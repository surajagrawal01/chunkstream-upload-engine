const Footer = () => {
    return (
        <footer className="border-t border-gray-200 bg-white px-4 py-4 text-center">
            <p className="text-xs text-gray-500">
                Learning project — chunked uploads and HTTP range streaming
            </p>
        </footer>
    );
};

export default Footer;